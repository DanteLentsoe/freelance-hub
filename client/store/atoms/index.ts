import { atom } from "recoil";

export const addClientModal = atom({
  key: "addClientModal",
  default: false,
});

export const addProjectModal = atom({
  key: "add-Project-Modal",
  default: false,
});
