export interface Projects {
  id: string;
  clientId: string;
  name: string;
  description: string;
  status: string;
}

export interface Client {
  id: string;
  name: string;
  email: string;
  phone: string;
}
